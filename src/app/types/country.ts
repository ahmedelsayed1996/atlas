export interface Country {
    id: number;
    name: string;
    logo: string | null;
}
export interface State {
    id: number;
    name: string;
}
export interface City {
    id: number;
    name: string;
}
